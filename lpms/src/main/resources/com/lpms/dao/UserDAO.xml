<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lpms.dao.UserDAO">
    <insert id="insertUser">
        INSERT INTO user (user_name, user_password, user_phone_num, user_age, user_sex, role_id)
        VALUES (#{userName}, #{userPassword}, #{userPhoneNum}, #{userAge}, #{userSex},#{roleId})
    </insert>
    <update id="updateUser">
        UPDATE user SET
        user_name = #{userName},
        user_password = #{userPassword},
        user_phone_num = #{userPhoneNum},
        user_age = #{userAge},
        user_sex = #{userSex}
        WHERE user_id = #{userId}
    </update>
    <delete id="deleteUserByName">
        DELETE FROM user WHERE user_name = #{userName}
    </delete>

    <select id="getUserById" parameterType="int" resultType="com.lpms.pojo.User">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>
<!--    User getUserByName(@Param("userName") String userName);-->
    <select id="getUserByName" resultType="com.lpms.pojo.User">
        SELECT * FROM user WHERE user_name=#{userName}
    </select>

<!--    查询监测人员User getMonUserById(int monUserId);-->
    <select id="getMonUserById" resultType="com.lpms.pojo.User">
        SELECT * FROM user WHERE user_id = #{monUserId} and role_id=3
    </select>

</mapper>
